{
  "name": "@ocshield/bridge",
  "version": "0.5.0",
  "description": "TypeScript bridge for OpenClaw Shield — connects Zig security engine to OpenClaw plugin API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "src/",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["openclaw", "security", "shield", "plugin", "guardrail", "pii", "secrets"],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/agentsoflearning/zig-oc-sheild"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
